{
  "name": "crm-backend",
  "version": "1.0.0",
  "description": "Booking management backend",
  "main": "dist/server.js",
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "scripts": {
    "dev": "dotenv -e .env.local -- tsx watch src/server.ts",
    "dev:development": "dotenv -e .env.development -- tsx watch src/server.ts",
    "build": "tsc",
    "postinstall": "prisma generate",
    "start": "node dist/server.js",
    "lint": "eslint src --ext .ts",
    "prisma:migrate": "dotenv -e .env.local -- prisma migrate dev",
    "prisma:generate": "prisma generate",
    "prisma:seed": "dotenv -e .env.local -- prisma db seed",
    "prisma:studio": "dotenv -e .env.local -- prisma studio",
    "prisma:migrate:development": "dotenv -e .env.development -- prisma migrate dev",
    "prisma:seed:development": "dotenv -e .env.development -- prisma db seed",
    "prisma:studio:development": "dotenv -e .env.development -- prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.3",
    "pusher": "^5.3.4"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^20.14.9",
    "dotenv-cli": "^11.0.0",
    "prisma": "^5.22.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}
